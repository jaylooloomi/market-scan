"""price-mcp — price/volume safety-net sensor.

Per design: Price is a SAFETY NET, not a primary signal. A limit-up cluster
means the leading sensors (News/Data/Policy/Roadmap) already MISSED the early
signal. It exists to catch those misses, not to chase momentum.
"""
