"""Reviewer agent substrate (Phase 2): causal-tree schema, history RAG glue,
and prompt builders. The actual reasoning runs on Claude (via the Claude Code
subagent in the plugin, or the Anthropic SDK in headless mode).
"""
