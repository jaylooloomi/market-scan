{
  "_comment": "Plugin MCP servers. Launched via `uvx --from git+<repo>` so a marketplace install just needs `uv` on PATH — uv builds market-scan + deps into an ephemeral, cached env at first launch (no manual pip, no PyPI publish required). LOCAL DEV: `pip install -e .` then swap a server to {\"command\":\"python\",\"args\":[\"-m\",\"market_scan_mcp.news.server\"]} to test working-tree code instead of the pushed version. FinMind token optional (.env); FRED/TWSE/RSS/crash-watch work without it.",
  "mcpServers": {
    "market-scan-news": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/jaylooloomi/market-scan", "market-scan-news-mcp"]
    },
    "market-scan-data": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/jaylooloomi/market-scan", "market-scan-data-mcp"]
    },
    "market-scan-price": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/jaylooloomi/market-scan", "market-scan-price-mcp"]
    },
    "market-scan-policy": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/jaylooloomi/market-scan", "market-scan-policy-mcp"]
    },
    "market-scan-roadmap": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/jaylooloomi/market-scan", "market-scan-roadmap-mcp"]
    }
  }
}
