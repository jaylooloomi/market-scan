# Canonical dependency list lives in pyproject.toml. This mirrors the CORE deps
# so `pip install -r requirements.txt` works. Prefer `pip install -e .` (installs
# the package + these deps). Optional extras: ".[agents]" (anthropic + chromadb
# for vector RAG / LLM reviewer), ".[schedule]" (apscheduler).
pandas>=2.0
yfinance>=0.2
requests>=2.31
mcp>=1.0
feedparser>=6.0
pytrends>=4.9
python-dotenv>=1.0
beautifulsoup4>=4.12
lxml>=5.0
jieba>=0.42
